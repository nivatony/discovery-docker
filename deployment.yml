apiVersion: app/v1
Kind: Deployment
metadata:
    name: nginx-deployement
spec:
    selector:
        matchlabels:
            app: nginx
    replicas: 2
    template:
        metadata:
            labels:
                app: nginx
        spec:
            containers:
            - name: nginx
              image: nginx
              ports:
              - containerPort: 80